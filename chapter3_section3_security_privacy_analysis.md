# 第三章 系统设计

## 3.3 安全性分析与隐私保护讨论

本节从参数隔离、功能加密、数值聚合、消息完整性和掉线恢复五个方面分析所提出方法的安全性质，并明确当前原型能够验证的范围。需要区分两类结论：一类是由参数流向、签名验证、定点编码和恢复阈值等实现直接支持的协议结论；另一类是依赖具体 DMCFE-IP 构造及其安全证明的密码学结论。当前仓库中的 chunked_reference 和 toy_mcfe 后端属于参考实现，不能单独证明后者。

### 3.3.1 安全分析目标与假设

设第 \(t\) 轮的参与客户端集合为 \(\mathcal{A}_t\)，服务器收到的共享更新由

\[
\Delta_{i,t}^{s}
=
\widetilde{\boldsymbol{\theta}}_{i,t}^{s}
-
\boldsymbol{\theta}_{t}^{s}
\]

给出。本文采用诚实但好奇的服务器模型：服务器按照协议选择客户端、广播共享参数并执行聚合，但会保存历轮密文、签名权重、参与集合和最终模型，并尝试从这些信息推断某一客户端的独立更新或本地数据。客户端默认遵循本地训练和消息格式，但允许在密钥共享、密文上传或恢复响应阶段掉线。可信初始化组件负责公共参数发布和身份注册，不被建模为能够单独解密所有客户端更新的集中式数据持有者。

在该模型下，系统的安全目标包括：

1. 原始样本和标签始终留在客户端本地；
2. 个性化参数不进入服务器聚合，也不在客户端之间交换；
3. 对共享更新的明文输出限于预先授权的样本加权函数；
4. 轮次、参与者集合和样本权重不能被未授权地修改或跨轮次重放；
5. 在满足恢复阈值时维持聚合可用性，在阈值不足时拒绝产生不完整模型。

本文暂不把恶意客户端、服务器与密钥持有者串谋、后门攻击、流量分析防护和差分隐私纳入已证明的安全范围。这些攻击会改变对手能力或隐私定义，必须通过独立的安全模型和实验进行评估。

### 3.3.2 个性化参数隔离性分析

根据第 3.2 节的参数划分，模型状态集合满足

\[
\mathcal{K}^{s}\cap\mathcal{K}^{p}=\varnothing,
\qquad
\mathcal{K}^{s}\cup\mathcal{K}^{p}=\mathcal{K}.
\]

服务器维护共享表示 \(\boldsymbol{\theta}^{s}_t\)，而客户端 \(C_i\) 按客户端标识保存自己的预测头 \(\boldsymbol{\theta}^{p}_{i,t}\)。本地训练完成后，只有

\[
\Delta_{i,t}^{s}
=
\widetilde{\boldsymbol{\theta}}_{i,t}^{s}
-
\boldsymbol{\theta}_{t}^{s}
\]

被向量化、裁剪和编码；\(\boldsymbol{\theta}^{p}_{i,t}\) 不构造密文，也不参与服务器加权平均。因此，即使服务器能够看到每轮更新后的共享模型，也不能通过该聚合接口直接读取任意客户端的个性化预测头。

这一性质由实现中的两层约束共同保证。第一，FedRepClient.train 先固定共享表示训练个性化头，再固定个性化头训练共享表示，避免个性化头被误当作全局参数更新。第二，FedRepAPI 只使用 shared_state_keys 进行聚合，并以客户端 ID 持久化个性化状态。相关测试可以验证参数键集合和状态回写行为。

个性化隔离并不等价于完整的模型隐私。共享表示仍然由多个客户端的本地数据训练，其更新可能包含类别、特征或成员信息。因而本节的结论是“个性化参数未通过聚合接口暴露”，而不是“共享表示不泄露任何训练信息”。

### 3.3.3 功能加密下的更新机密性

对共享更新进行向量化和定点编码后，客户端得到

\[
\boldsymbol{z}_{i,t}
=
\operatorname{round}\!\left(
q\operatorname{clip}
\left(\operatorname{vec}_{\mathcal{K}^{s}}(\Delta_{i,t}^{s}),-B,B\right)
\right).
\]

客户端上传密文

\[
\boldsymbol{c}_{i,t}
=
\mathsf{Enc}(pp,sk_i,\boldsymbol{z}_{i,t},\tau_t),
\]

其中 \(\tau_t\) 绑定轮次、参与者集合、模型版本和分块标识。服务器申请的功能只对应样本权重向量 \(\boldsymbol{w}_t\)，解密输出为

\[
\boldsymbol{u}_t
=
\sum_{i\in\mathcal{A}_t}w_{i,t}\boldsymbol{z}_{i,t}.
\]

若所采用的 DMCFE-IP 方案满足正确性，以及与目标构造相匹配的消息机密性和抗串谋安全定义，则服务器在获得功能解密结果后只能得到该授权线性函数的输出，而不能从单个密文中恢复任意 \(\boldsymbol{z}_{i,t}\)。这一结论需要具体密码方案的安全参数、密钥生成过程和安全证明支持，不能由接口名称或参考代码自动推出。

即使功能加密满足上述定义，聚合输出本身仍可能造成推断风险。当 \(|\mathcal{A}_t|\) 很小、某个客户端的权重占比过大，或服务器能够控制多轮参与集合时，加权和可能近似暴露单个更新。因此，功能加密解决的是“未经授权读取单客户端密文”的问题，不自动解决小群体聚合、差分攻击和跨轮次关联攻击。实际部署需要设置最小参与人数、限制重复查询，并结合差分隐私等机制进一步降低聚合输出泄露。

### 3.3.4 聚合正确性与定点误差分析

客户端在裁剪后进行定点编码，服务器通过功能解密得到

\[
\boldsymbol{u}_t
=
\sum_{i\in\mathcal{A}_t}w_{i,t}\boldsymbol{z}_{i,t},
\qquad
W_t=\sum_{i\in\mathcal{A}_t}w_{i,t},
\]

并计算

\[
\widehat{\Delta}^{s}_t
=
\operatorname{unvec}
\left(
\frac{\boldsymbol{u}_t}{qW_t}
\right).
\]

令 \(\Delta^{\mathrm{plain}}_t\) 表示未裁剪浮点更新的样本加权平均，\(\Delta^{\mathrm{clip}}_t\) 表示裁剪后的加权平均，则总误差可以分解为

\[
\left\|
\widehat{\Delta}^{s}_t-
\Delta^{\mathrm{plain}}_t
\right\|_{\infty}
\leq
\left\|
\Delta^{\mathrm{clip}}_t-
\Delta^{\mathrm{plain}}_t
\right\|_{\infty}
+
\frac{1}{2q}.
\]

第二项是四舍五入误差上界；当所有坐标均未触发裁剪时，每个聚合坐标相对于明文加权平均的误差不超过 \(1/(2q)\)。第一项反映裁剪带来的偏差，通常随着阈值 \(B\) 增大而减小，但过大的 \(B\) 会扩大整数明文和密文运算的范围。系统还需满足

\[
\max_{i,r}|z_{i,t,r}|W_t<2^{63}
\]

等整数范围约束，以避免加权求和溢出。

因此，聚合正确性应分为两个层次。密码学正确性要求功能解密输出等于编码向量的加权和；数值正确性要求反量化结果与明文参考聚合之间的误差处于可接受范围。当前 OptimizedDMCFEIPAggregator 能够记录最大绝对误差、平均绝对误差和裁剪比例，测试也覆盖了参考后端与小规模 toy 后端的数值一致性。这些证据支持定点聚合的实现正确性，但不替代具体功能加密方案的正确性证明。

对于模型状态中的整数缓冲区，系统只在参与客户端取值一致时复制该状态，不对其执行浮点加权平均。该约束避免了将计数器、索引或其他离散状态错误地解释为可线性聚合的参数。

### 3.3.5 权重完整性与重放防护

样本数不仅决定联邦平均的统计权重，也决定功能密钥所授权的线性函数。客户端对以下消息进行签名：

\[
m_{i,t}=(t,\mathcal{A}_t,i,w_{i,t}),
\]

其中 \(t\) 为通信轮次，\(\mathcal{A}_t\) 为有序参与者集合，\(i\) 为客户端标识，\(w_{i,t}>0\) 为本地样本数。服务器在生成或组合功能密钥前验证参与客户端的签名，并检查客户端集合、权重和轮次是否与当前任务一致。

该绑定提供三项完整性保护。其一，攻击者不能在不破坏签名的情况下把某个客户端的样本权重替换为另一数值；其二，旧轮次签名包含不同的 \(t\)，不能直接用于当前轮次；其三，签名包含完整参与集合，来自不同采样集合的有效消息不能被简单拼接。密文标签 \(\tau_{t,b}\) 进一步将模型版本和分块编号纳入加密上下文，防止不同参数位置的密文被混用。

但是，签名只证明消息来自登记客户端且未被篡改，不能证明客户端提交的更新符合训练结果，也不能阻止恶意客户端上传后门更新或故意选择异常样本权重。对恶意客户端的鲁棒聚合、异常检测和身份吊销需要在本节之外单独设计。密钥轮换、签名公钥分发和撤销列表也必须由部署系统补充，否则签名验证无法覆盖密钥泄露后的风险。

### 3.3.6 掉线恢复安全性

为分析掉线时序，定义

\[
\mathcal{U}_{4,t}
\subseteq
\mathcal{U}_{3,t}
\subseteq
\mathcal{U}_{2,t}
\subseteq
\mathcal{A}_t,
\]

其中 \(\mathcal{U}_{2,t}\) 表示完成密钥共享的客户端，\(\mathcal{U}_{3,t}\) 表示完成密文上传的客户端，\(\mathcal{U}_{4,t}\) 表示完成恢复响应的客户端。若客户端属于 \(\mathcal{U}_{2,t}\setminus\mathcal{U}_{3,t}\)，其成对掩码可能残留在在线密文中；若客户端属于 \(\mathcal{U}_{3,t}\setminus\mathcal{U}_{4,t}\)，其密文已经上传但对应的恢复材料尚未提交。协议分别重构早期掉线客户端的临时密钥材料和晚期掉线客户端的掩码种子，以消除这些残留项。

在恢复响应数满足

\[
|\mathcal{U}_{4,t}|\geq\rho
\]

时，服务器才能获得足够的秘密共享份额，并将恢复后的功能密钥或掩码组合用于实际上传客户端集合 \(\mathcal{U}_{3,t}\) 的聚合。当 \(|\mathcal{U}_{4,t}|<\rho\) 时，服务器必须中止本轮，不应使用不完整的恢复材料生成共享模型。该规则同时提供了可用性边界和失败可诊断性。

掉线恢复的安全性依赖阈值秘密共享、参与者之间的非串谋假设以及恢复消息的认证。恢复过程本身主要解决掩码抵消和协议可用性，并不自动防止服务器与足够数量的客户端串谋来重构某一客户端的秘密。当前仓库的掉线测试验证了 \(\mathcal{U}_2\)、\(\mathcal{U}_3\)、\(\mathcal{U}_4\) 的集合关系、早期和晚期掉线分支以及阈值不足时的失败行为；它们证明的是协议流程和数值恢复，不是对任意密码实例的形式化抗串谋证明。

### 3.3.7 隐私泄露边界与残余风险

在本文威胁模型下，仍需关注以下残余风险。

1. **聚合结果泄露。** 功能密钥只限制可计算函数，但授权函数的输出仍然包含共享更新信息。参与客户端数量过少或权重分布过于集中时，服务器可能通过差分比较推断单个客户端的变化。
2. **共享表示泄露。** 个性化头虽然保持本地化，但共享表示更新仍可能编码类别特征、成员信息或训练样本统计规律。参数隔离不能替代梯度反演防护。
3. **跨轮次关联。** 服务器可以保存多个轮次的聚合结果和参与集合。当客户端频繁单独参与或参与集合变化可预测时，历轮差分可能削弱聚合隐私。
4. **恶意客户端与串谋。** 恶意客户端可以提交异常更新，服务器与部分客户端也可能联合分析恢复材料。当前实现没有给出拜占庭鲁棒性或任意串谋下的安全证明。
5. **元数据泄露。** 客户端 ID、参与集合、样本数量、上传时间和密文长度可能暴露业务规模或参与模式。功能加密通常不隐藏这些通信元数据。
6. **参数与实现泄露。** 裁剪阈值、缩放因子、密文分块长度和错误统计会影响可观测的数值范围。定点编码限制了编码后的数值范围，但不提供差分隐私意义下的随机保护。

因此，本文不宣称当前方法满足 \((\varepsilon,\delta)\)-差分隐私，也不宣称能够抵抗所有模型反演、成员推断或流量分析攻击。若论文后续需要给出这些性质，应增加噪声校准、最小群体约束、跨轮次隐私预算和对应攻击实验，并将其作为独立安全机制报告。

### 3.3.8 参考实现限制与安全结论

当前实现可以支持的结论和仍需补充的证据如下表所示。

| 安全或正确性性质 | 当前证据 | 本文可采用的表述 |
| --- | --- | --- |
| 个性化参数隔离 | FedRepClient、FedRepAPI 及相关测试 | 个性化头不进入服务器共享参数聚合 |
| 定点加权聚合 | OptimizedDMCFEIPAggregator、数值测试和误差记录 | 参考后端上的编码聚合与明文结果满足误差界 |
| 权重完整性 | weight_signatures.py 和功能测试 | 签名绑定轮次、参与集合、客户端 ID 和样本权重 |
| 掉线恢复流程 | 独立的 dropout smoke tests | 阈值满足时可恢复协议所需掩码或密钥组合 |
| 单客户端更新机密性 | 需要安全 DMCFE-IP 构造及证明 | 在消息机密性和抗串谋假设成立时的条件性结论 |
| 生产级组合安全 | 当前尚无统一安全后端 | 不能据此宣称已完成生产部署或形式化安全证明 |

特别地，chunked_reference 用于高维向量的分块、裁剪和定点误差验证，toy_mcfe 用于小规模功能密钥接口和数值一致性检查；完整掉线恢复流程位于独立的协议验证路径中。三者尚未组合为经过安全审计的生产级 DMCFE-IP 实现。因此，本文第三节的最终安全结论应写成：在参数划分、签名验证、定点编码和恢复阈值均正确执行，且底层 DMCFE-IP 方案满足相应的正确性、消息机密性和抗串谋定义时，系统能够将服务器可获得的信息限制为授权的共享更新聚合值；当前代码已经验证协议接口、数值误差和部分恢复流程，但不能单独证明该条件性密码学结论。

