# 第四章 实验与结果分析

## 4.1 实验设置

本章通过图像分类实验评估个性化训练流程、聚合数值正确性和协议可用性。实验基于 FedML 单进程模拟框架运行，以预划分的非独立同分布 MNIST 数据作为主要任务。正式统一对比脚本将为各方法固定相同的数据划分、客户端采样顺序、模型初始化规则、批大小和学习率；本章已完成的早期 FedAvg 记录因缺少完整配置快照，仅用于数值回归。除模型指标外，实验还记录定点聚合误差、权重签名验证和掉线恢复状态。当前高维聚合后端属于非安全参考实现，本章不将其运行时间解释为生产级功能加密的密码运算开销。

### 4.1.1 实验目标与研究问题

实验围绕以下四个研究问题展开。

1. **RQ1：个性化参数划分在异构客户端上的分类表现如何？**  
   通过比较 FedAvg、FedRep 和已执行原型的测试准确率与测试损失，分析共享表示和本地预测头的参数划分是否适合当前非独立同分布任务。
2. **RQ2：定点参考聚合能否保持明文加权平均的数值结果？**  
   比较定点参考聚合输出与明文样本加权平均，报告最大绝对误差、平均绝对误差和裁剪比例，并分析缩放因子与裁剪阈值的影响。
3. **RQ3：协议在不同客户端掉线阶段能否正确恢复聚合？**  
   分别模拟密文上传前掉线、恢复响应前掉线和恢复份额不足三类情况，检查恢复结果、在线客户端集合和阈值不足时的中止行为。
4. **RQ4：隐私聚合引入了多少计算与通信代价？**  
   分别统计客户端编码或加密、功能密钥生成与组合、服务器解密或聚合的时间以及序列化消息字节数。当前参考后端只能提供定点处理和聚合流程的基准，真实密码开销需要在接入经过验证的 DMCFE-IP 后端后测量。

前两个问题评价方法的学习效果和数值可用性，后两个问题评价协议实现的恢复能力和系统代价。该划分避免使用单一分类准确率代替隐私聚合的正确性与效率证据。

### 4.1.2 数据集与非独立同分布设置

实验使用 FedML 提供的预处理 MNIST 联邦数据。每个输入样本为一幅 $28\times28$ 的灰度手写数字图像，分类标签为 0 至 9。当前数据包的统计信息如表 4-1 所示。

**表 4-1  MNIST 联邦数据统计**

| 项目 | 设置 |
| --- | ---: |
| 客户端数量 | 1000 |
| 类别数量 | 10 |
| 训练样本数量 | 61664 |
| 测试样本数量 | 7371 |
| 单客户端包含的数字类别 | 2 |
| 客户端样本量分布 | 幂律分布 |

该数据不是在实验启动时通过 Dirichlet 分布临时生成的。数据加载器直接读取训练集和测试集目录中的预划分 JSON 文件，每个客户端仅包含两类数字，且不同客户端的样本数量不均衡。因此，实验中的异质性同时来自标签分布偏移和样本数量不平衡。

配置文件虽然保留了 hetero 划分标记和数值为 0.5 的 partition_alpha 字段，但当前 MNIST 加载器不读取这两个字段来重新划分数据。为保证实验描述与实际执行一致，本文不把 0.5 表述为有效的 Dirichlet 浓度参数，而以预处理数据包的两类标签约束和幂律样本量作为非独立同分布设置。

每轮从 1000 个客户端中无放回采样 3 个客户端。采样器按照通信轮次生成确定性的客户端序列，使不同方法在同一轮使用相同的参与者集合。该设置有利于进行配对比较，但每轮参与比例仅为 0.3%，因此实验结果主要反映低参与率条件下的训练行为。

### 4.1.3 模型结构与个性化参数划分

分类模型采用面向 MNIST 的 CNN_DropOut 网络，其结构如表 4-2 所示。

**表 4-2  CNN 模型结构**

| 顺序 | 网络层 | 主要设置 |
| ---: | --- | --- |
| 1 | 二维卷积层 | 输入通道 1，输出通道 32，卷积核 $3\times3$ |
| 2 | 二维卷积层 | 输入通道 32，输出通道 64，卷积核 $3\times3$ |
| 3 | 最大池化层 | 核大小 2，步长 2 |
| 4 | Dropout | 丢弃概率 0.25 |
| 5 | 全连接层 linear_1 | $9216\rightarrow128$，ReLU 激活 |
| 6 | Dropout | 丢弃概率 0.50 |
| 7 | 全连接层 linear_2 | $128\rightarrow10$ |

模型共有 1199882 个可训练参数。本文将最后的 linear_2 层作为客户端个性化预测头，该层包含 1290 个参数；其余卷积层和 linear_1 层构成共享表示，共包含 1198592 个参数。FedAvg 聚合完整模型，FedRep 和已执行原型只聚合共享表示。对 FedRep 进行客户端测试前，系统按照客户端标识加载其本地预测头，使测试指标反映共享表示与个性化参数共同作用下的分类结果。

### 4.1.4 对比方法

主要对比方法如表 4-3 所示。

**表 4-3  主要对比方法及作用**

| 方法 | 参数组织 | 聚合方式 | 对比目的 |
| --- | --- | --- | --- |
| FedAvg | 全部参数共享 | 明文样本加权平均 | 提供统一全局模型基线 |
| FedRep | 共享表示与本地预测头 | 仅对共享表示执行明文加权平均 | 分离个性化机制带来的影响 |
| 已执行原型 | 共享表示与本地预测头 | 对共享更新执行裁剪、定点编码和定点参考聚合 | 验证参数划分、数值一致性与训练流程 |

仓库还提供 BatchCrypt 原型和成对掩码聚合原型，用于检查不同隐私聚合接口的数值正确性。其中，BatchCrypt 原型采用定点打包和小参数 Paillier 加法同态聚合，成对掩码方法利用符号相反的随机掩码抵消客户端更新。这两个原型当前运行在 FedAvg 参数组织下，与 FedRep 和已执行原型的个性化模型结构不同，因此只作为协议级补充对比，不用于单独归因个性化精度差异。

当前原型的配置值为 `chunked_reference`，运行诊断中的后端标签为 `chunked-reference`；二者均指不执行加密的参考实现。目标 DMCFE-IP 后端在接入并完成审查前不纳入方法性能或系统效率比较。对于仓库中没有可执行实现的方法，实验清单将其标记为未实现，而不使用 FedAvg 或已执行原型替代。正式结果表只纳入实际运行成功且实验语义一致的方法。

### 4.1.5 联邦训练参数

主要训练参数如表 4-4 所示。已完成的正式统一对比中，各方法使用相同的通信轮数、客户端采样序列、批大小和学习率；早期记录与正式结果的适用范围在第 4.2 节单独说明。

**表 4-4  联邦训练参数**

| 参数 | 设置 |
| --- | ---: |
| 通信轮数 | 50 |
| 客户端总数 | 1000 |
| 每轮参与客户端数 | 3 |
| 批大小 | 10 |
| 优化器 | SGD |
| 学习率 | 0.03 |
| 损失函数 | 交叉熵损失 |
| FedAvg 本地训练轮数 | 1 epoch |
| FedRep 个性化头训练轮数 | 1 epoch |
| FedRep 共享表示训练轮数 | 1 epoch |
| 主配置测试频率 | 每 1 个通信轮次 |
| 基础随机种子 | 0 |
| 正式对比随机种子 | 0、1、2 |

当前 SGD 实现只传入学习率，没有把 YAML 中设置为 0.001 的 weight_decay 参数传递给优化器，因此实际权重衰减为 0。本文按照有效执行参数报告实验设置。FedRep 的两个本地阶段分别重新构造优化器：第一阶段固定共享表示并训练个性化预测头，第二阶段固定预测头并训练共享表示。

正式方法比较按主配置每轮执行一次测试，得到三个方法、三个随机种子的完整 50 轮轨迹。早期的 50 轮本地集成配置只在第 0 轮和第 49 轮测试，适合检查长流程能否完成，不纳入正式曲线和统计汇总。

### 4.1.6 定点参考聚合与掉线恢复参数

已执行原型的聚合与独立掉线恢复参数如表 4-5 所示。

**表 4-5  定点参考聚合与独立恢复验证参数**

| 参数 | 设置 |
| --- | --- |
| 聚合模式 | optimized，全共享状态聚合 |
| 当前后端配置值 | chunked_reference |
| 当前后端运行标签 | chunked-reference |
| 后端安全标记 | False |
| 定点缩放因子 $q$ | 16384 |
| 坐标裁剪阈值 $B$ | 8.0 |
| 分块长度 $L$ | 65536 |
| 样本权重签名 | 启用 |
| 独立恢复验证的配置阈值 $\rho$ | 2 |
| 默认掉线注入 | 无 |

共享表示包含 1198592 个浮点参数。聚合器按照模型状态键逐层展平和分块，并对每个参数张量计算样本加权整数和。分块长度限制单次处理的坐标数量，但不改变聚合结果。配置中的 256 位模数只供 toy_mcfe 小规模接口检查使用；chunked_reference 后端不执行模运算或密文加密。dmcfe_validation_dim 字段仅服务于旧的前缀 smoke 路径，在 optimized 模式下不限制共享表示的聚合维度。

默认分类训练不注入掉线。表 4-5 中的恢复阈值用于独立的恢复验证路径，不参与 optimized 高维参考聚合。掉线恢复实验构造三类集合：完成密钥共享的客户端集合、完成密文上传的客户端集合和提交恢复响应的客户端集合。实验分别设置早期掉线、晚期掉线和恢复响应数小于阈值三种场景。高维 optimized 参考聚合与完整掉线恢复当前尚未组合为统一的生产级密码后端，因此两类实验分别报告，不把独立验证结果解释为完整系统的组合安全性。

### 4.1.7 实验环境

实验环境于 2026 年 9 月 7 日在本地设备上核验；2026 年 9 月 18 日的正式运行另保存了逐运行环境快照，具体配置如表 4-6 所示。

**表 4-6  软硬件环境**

| 类型 | 配置 |
| --- | --- |
| 操作系统 | Windows 11，版本 10.0.22631 |
| 处理器 | AMD Ryzen 7 5800H，8 核 16 线程 |
| 内存 | 15.9 GiB |
| 图形处理器 | NVIDIA GeForce RTX 3060 Laptop GPU，6144 MiB |
| 显卡驱动 | 576.52 |
| Python | 3.9.25 |
| FedML | 0.8.30 |
| PyTorch | 2.8.0+cu129 |
| CUDA | 12.9 |
| NumPy | 1.26.4 |
| PyYAML | 5.4.1 |
| cryptography | 50.0.0 |
| SymPy | 1.14.0 |

实验使用 D:\anaconda\envs\fedmlnew\python.exe 解释器和编号为 0 的 GPU。FedML 采用单进程模拟后端，客户端训练在同一主机上依次执行。因此，本实验能够控制软件和硬件环境，但其运行时间不代表跨设备网络中的端到端延迟。

### 4.1.8 评价指标与统计方法

模型性能使用训练准确率、训练损失、测试准确率和测试损失评价。设客户端 $C_i$ 的测试样本数为 $n_i^{\mathrm{test}}$，正确分类数为 $a_i$，累计测试损失为 $\ell_i$，则总体测试准确率和平均损失分别为

\[
\operatorname{Acc}_t
=
\frac{\sum_{i=1}^{K}a_i}
{\sum_{i=1}^{K}n_i^{\mathrm{test}}},
\qquad
\operatorname{Loss}_t
=
\frac{\sum_{i=1}^{K}\ell_i}
{\sum_{i=1}^{K}n_i^{\mathrm{test}}}.
\]

聚合正确性通过定点参考聚合输出与明文样本加权平均之间的误差衡量。设共享更新维度为 $d$，两种聚合结果分别为 $\widehat{\boldsymbol{\delta}}_t$ 和 $\boldsymbol{\delta}^{\mathrm{plain}}_t$，则

\[
E_{\max,t}
=
\left\|
\widehat{\boldsymbol{\delta}}_t
-
\boldsymbol{\delta}^{\mathrm{plain}}_t
\right\|_{\infty},
\]

\[
E_{\mathrm{mean},t}
=
\frac{1}{d}
\left\|
\widehat{\boldsymbol{\delta}}_t
-
\boldsymbol{\delta}^{\mathrm{plain}}_t
\right\|_{1}.
\]

同时记录触发裁剪的坐标比例，用于区分定点舍入误差与裁剪偏差。新版逐轮 CSV 保存最大误差、平均误差、裁剪比例、坐标数、块数和定点参数；早期运行生成的 CSV 缺少其中部分字段，不能与正式对比结果直接合并。

掉线实验记录实际上传密文的客户端数量、参与者集合、恢复阈值、恢复误差、签名验证数量以及当前轮次是否成功。阈值不足导致的协议中止应作为预期失败单独统计，不能从样本中删除。早期运行曾将 $1-3/1000=0.997$ 写入 `dropout_rate`，该数值表示未参与训练的客户端比例，不能用于评价掉线恢复。新版记录器将采样参与比例单独写为 `participation_rate=0.003`；仅当独立 smoke 协议实际执行恢复时，才填写 `dropout_rate`、`protocol_dropout_rate`、`recovery_threshold` 和 `recovery_error`。正式 optimized 参考聚合不执行该恢复路径，以上恢复字段留空。

计算和通信开销应分别报告各密码阶段的墙钟时间、峰值内存和序列化字节数。新版结果文件为 `chunked-reference` 后端记录定点编码、整数聚合和本地总耗时；这些时间不含加密、功能密钥生成、解密或网络传输。密码阶段耗时、序列化字节数和峰值内存仍无有效测量值，正式结果中保持空白，不用于功能加密开销比较。

为降低单次随机初始化造成的偶然性，正式对比实验已使用随机种子 0、1 和 2，分别完成 FedAvg、FedRep 和 FedRep+定点参考聚合的 50 轮运行。客户端采样由通信轮编号固定，因此相同轮次的客户端集合在全部方法和种子间一致；配置与环境快照均已保存。第 4.2 节以种子为重复单位，报告第 49 轮指标的均值、样本标准差及同种子配对差值。该标准差不包含客户端采样序列变化的影响；三个种子也不足以支撑稳健的显著性或方法普遍优劣结论。
