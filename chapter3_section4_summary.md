# 第三章 系统设计

## 3.4 本章小结

本章围绕非独立同分布数据下的个性化建模需求和联邦训练过程中的更新泄露风险，给出了面向目标 DMCFE-IP 后端的隐私保护个性化联邦学习设计。首先，本章建立了由协调服务器、多个客户端和可信初始化组件构成的系统模型，明确了数据分布、模型参数划分、通信轮次及威胁假设。该模型将网络参数划分为跨客户端学习的共享表示和保留在本地的个性化预测头，使系统能够在学习共性特征的同时适应不同客户端的数据分布。

在此基础上，本章给出了个性化联邦学习与目标 DMCFE-IP 聚合方法。客户端采用双阶段训练过程，依次更新个性化预测头和共享表示，并且只上传共享表示的更新。为适配功能加密的整数明文空间，共享更新在加密前经过固定顺序向量化、坐标裁剪、定点量化和分块处理。目标安全后端利用与样本权重绑定的功能密钥恢复编码更新的加权和，再通过反量化和参数重构完成共享模型更新。权重签名将通信轮次、参与者集合、客户端身份和样本数量绑定到同一消息，掉线恢复协议则根据密钥共享、密文上传和恢复响应三个阶段区分客户端状态，并在在线响应数达到阈值时恢复聚合所需材料。

本章进一步分析了该方法的参数隔离性、更新机密性、聚合正确性、消息完整性和掉线可用性。个性化预测头不进入加密与聚合流程；定点舍入误差可以与裁剪偏差分别度量；轮次标签和权重签名降低了密文混用、权重篡改和跨轮次重放的风险。上述机密性结论以底层 DMCFE-IP 方案满足相应的消息机密性和抗串谋安全定义为前提。当前参考后端能够验证参数流向、数值等价性、签名校验和掉线恢复流程，但不能替代生产级密码实现的形式化安全证明，也不提供差分隐私、恶意客户端防御或通信元数据隐藏。

综上，本章完成了从个性化模型训练到加密聚合和安全边界分析的方法设计，为后续实验提供了可验证的技术对象。后续实验应分别考察非独立同分布条件下的全局与个性化模型性能、定点量化误差与裁剪比例、加密聚合的计算和通信开销，以及不同掉线阶段和恢复阈值下的协议可用性，从而验证所提出方法的有效性、数值正确性和实际代价。
