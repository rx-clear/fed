# 第四章 实验与结果分析
## 4.4 本章小结

本章围绕三方法训练对比、定点聚合数值正确性、阈值恢复协议和参数敏感性开展实验验证。正式矩阵在预划分的非独立同分布 MNIST 数据上完成 3 个方法、3 个随机种子、每组 50 轮，共得到 450 条唯一逐轮记录；所有运行在相同轮次使用相同客户端集合，并保存配置与环境快照。缺少完整配置快照的早期 FedAvg 记录仅用于数值回归，不用于正式方法比较。模型指标、聚合误差、签名与恢复状态、参考后端本地耗时分别报告，避免把不同实验路径的证据合并解释。

对于模型性能，三个种子的第 49 轮测试准确率均值分别为 FedAvg 60.6521%、FedRep 53.1090% 和 FedRep+定点参考聚合 56.8173%。相对于 FedAvg，后两者的同种子配对准确率差均值分别为 $-7.5431$ 和 $-3.8348$ 个百分点。本次配置未观察到 FedRep 或参考方法的终点准确率优势；三个种子仍不足以支持普遍的算法排名或统计显著性结论。早期单次运行的 78.5104% 终点准确率来自另一组本地集成记录，不与正式矩阵合并。

对于定点聚合，正式参考方法的 150 条记录在 $q=2^{14}$、$B=8$ 下均未发生裁剪，坐标级最大绝对误差不超过 $3.0482\times10^{-5}$。早期 20 轮配对轨迹仍提供独立的数值回归证据。补充的 300 条定点消融记录表明：在构造更新且未发生裁剪时，提高缩放因子会降低舍入误差；当裁剪阈值从 8 降至 0.5 时，裁剪比例从 0 增至 50.9%，相对于未裁剪浮点参考的最大绝对误差均值从 $2.440\times10^{-5}$ 增至 1.655；分块长度不改变参考聚合输出。这些证据支持当前参数下的数值近似与误差来源区分，不说明训练准确率差异的原因，也不把构造更新上的工作点表述为任意训练任务的最优配置。

对于协议可用性，独立的小规模掉线恢复验证在恢复响应数达到阈值 2 时得到与明文整数加权和完全一致的结果，在响应数降至 1 时按预期中止。该结果支持“份额达到阈值才恢复”的控制逻辑，以及权重签名对参与者、轮次和权重绑定的接口执行。由于高维 `optimized` 聚合与完整掉线恢复仍是两条独立验证路径，本章不据此宣称组合系统已经具有生产级容错能力、服务器—客户端抗串谋安全性或完整的密码学安全证明。

最后，计算开销分析只覆盖 `chunked-reference` 后端的 CPU 本地定点编码和分块整数加权循环。该后端不执行加密、功能密钥生成与组合、解密、密文序列化或网络传输；其耗时只能说明分块实现的参数敏感性，不能作为功能加密计算、通信或端到端系统延迟的证据。后续应在接入经过审查的 DMCFE-IP 后端后，按客户端编码/加密、功能密钥、服务器解密、序列化通信和峰值内存分别测量真实代价，并在相同的三种子训练设置下增加安全后端对照。只有完成这些测量，才能把本章的数值正确性与流程验证扩展为安全系统效率结论。
